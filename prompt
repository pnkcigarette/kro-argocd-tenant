Read AGENTS.md first and follow its
KRO/Kyverno rules and testing conventions. This builds on the label
model just landed: CRs live in the tenant's <tenant>-argocd-mgmt
namespace, identity binds via the mgmt namespace's platform-stamped
labels (example.com/tenant, annotation example.com/git-namespace),
never via name parsing.

TASK: rework ArgoCDProjectRepository for mgmt-namespace residency with
team-owned Vault trust and pull-mode secret replication
(mittwald/kubernetes-replicator) into the argocd namespace.

1. CR RESIDENCY + IDENTITY:
   - ArgoCDProjectRepository instances live in the mgmt namespace.
     spec.tenant must equal the namespace's example.com/tenant label
     (policy). spec.project (when present) must name an ArgoCDProject
     that exists in the SAME mgmt namespace (referential rule keys on
     labels + namespace, not names).
   - Keep the existing variants: project-scoped repository entries
     (spec.project required), tenant-scoped repo-creds (spec.project
     forbidden), credType none attach. Dup/immutability rules re-key
     to (tenant, project?, name) across ALL mgmt namespaces.

2. VAULT SIDE MOVES TO THE MGMT NAMESPACE (team-owned trust):
   - The RGD renders the VaultStaticSecret IN the mgmt namespace
     (same namespace as the CR), referencing a per-tenant VaultAuth
     that also lives there (the VaultAuth/VaultConnection themselves
     are onboarding/seed-provided, NOT rendered by this RGD — assume
     names by convention, document them).
   - The VSS destination secret is created in the mgmt namespace and
     must carry, via VSS spec.destination annotations, the
     replicator's pull-consent annotations: replication-allowed:
     "true" and replication-allowed-namespaces: "argocd" (check the
     exact annotation keys against the replicator's docs for the
     pinned version).
   - Vault path convention unchanged (<mount>/<pathPrefix>/... per
     README); the k8s auth role binding to the mgmt-ns SA is the
     team's Vault-side task — document, don't implement.

3. STUB SECRET IN argocd (KRO-rendered, replicator fills data):
   - The RGD renders a stub Secret in the argocd namespace carrying
     ALL meaning-bearing metadata: argocd.argoproj.io/secret-type
     label (repository or repo-creds), data-free, project field
     handling per variant (project-scoped entries embed
     project: <tenant>-<project>; repo-creds embed none), tenant/
     project labels, url, plus the replicator pull annotation
     replicator.v1.mittwald.de/replicate-from:
     <mgmt-ns>/<vss-secret-name>.
   - CRITICAL: the stub template must NOT include a data field. Do
     not add fields that would make KRO claim ownership of data.
     (SSA coexistence of replicator-written data with KRO reconcile
     is a live-verify item — add a TODO/test note, do not assume.)
   - For credType none: no VSS, no replication annotations — render
     the metadata-only secret directly in argocd as today.

4. GUARDS (platform-policies):
   - sourceRepos/repo URL allowlist: repo CR spec.url (and
     ArgoCDProject spec.sourceRepos entries) must match the mgmt
     namespace's example.com/git-namespace annotation prefix or an
     approved-registry list; platform principal excepted. This rule
     is security-load-bearing (tenant repo-creds match by URL prefix
     with no project scoping) — say so in its comments.
   - argocd-namespace secret guard: secrets written by the
     replicator SA may only target Secrets that already exist with
     KRO's ownership metadata, and may never carry
     argocd.argoproj.io/secret-type: cluster. Deny push-mode:
     no secret in argocd may carry replicate-to/push annotations.
   - Keep the referential teardown rules working with the new
     residency (repo CRs block project deletion per existing
     direction — one direction only, per AGENTS.md).

5. TESTS (tests/policy-tests.sh, existing conventions):
   - Deny: repo CR tenant mismatching mgmt-ns label; repo URL outside
     git-namespace prefix; repo-creds with spec.project; repository
     without spec.project (non-creds variants); dup (tenant,project,
     name) across mgmt namespaces; secret-type cluster via
     replication path; push-mode annotations in argocd ns.
   - Render: VSS lands in mgmt ns with consent annotations; stub
     lands in argocd with replicate-from, correct labels/project
     field, and NO data key in the template; credType none renders
     metadata-only secret with no replication annotations.
   - The replicator itself is NOT installed on the validate cluster —
     test rendered metadata, not data flow; list live replication +
     KRO/SSA data-field coexistence as documented follow-up
     verification items in the README/AGENTS.md testing notes.

Verify per AGENTS.md: RGDs Active, preflight passes, full test suite
passes.
