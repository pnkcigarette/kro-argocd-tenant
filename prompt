Work in the kro-argocd-tenant repo. Read AGENTS.md first and follow its
KRO/Kyverno rules and testing conventions.

TASK: move identity from name-parsing to platform-guarded labels, adopt
the new namespace layout, add Rancher project auto-placement, and wire
the sync-impersonation SA into control-plane namespaces.

1. NAMING (convention only — no policy may parse names for identity):
   - Source namespaces: <tenant>-<project> (KRO-rendered).
   - Mgmt namespace: <tenant>-argocd-mgmt (pre-existing birthright,
     onboarding-created; RGDs/policies must assume it exists).
   - Add a Kyverno rule: len(tenant) + len(project) <= 62 (namespace
     63-char limit), and a reserved-slug denylist for tenant values
     (argocd, kube, cattle, default, tenant).

2. LABEL MODEL:
   - Machine identity is labels, never names. The mgmt namespace
     carries platform-stamped example.com/tenant,
     example.com/assignee-group, field.cattle.io/projectId
     (label + annotation forms), and annotation
     example.com/git-namespace. Add a policy making these immutable to
     non-platform principals.
   - Rewrite all identity-binding rules to key on labels: a CR's
     spec.tenant must equal its namespace's example.com/tenant label;
     spec.assigneeGroup must equal the namespace's assignee-group
     label. Delete any rule that derives identity from namespace name
     prefixes.
   - Add a cross-namespace duplicate check: (tenant, name) must be
     unique across all ArgoCDProject instances in all mgmt namespaces.

3. RANCHER PLACEMENT (teams supply nothing):
   - The project RGD stamps the rendered source namespace with the
     tenant/project label pair AND example.com/mgmt-namespace:
     <instance's own namespace> (verify schema metadata namespace is
     referenceable in template CEL; if not, note it and fall back to
     policy lookup by labels).
   - New classic ClusterPolicy (mutate needs classic, not VP): on
     Namespace CREATE bearing example.com/mgmt-namespace, apiCall the
     named mgmt namespace and inject its field.cattle.io/projectId
     annotation + label into the new namespace at admission. Restrict
     the rule to namespaces created by the KRO controller SA.

4. IMPERSONATION SA (impersonation is already forced instance-wide):
   - SA name convention: argocd-<tenant>-<project>.
   - The project RGD must render this ServiceAccount plus a RoleBinding
     in the SOURCE namespace (rights: Applications/ApplicationSets and
     the resources the bootstrap/root sync applies there), because the
     source namespace is itself a sync destination on the control
     plane and every destination now requires the SA to exist.
   - Render destinationServiceAccounts on the AppProject referencing
     argocd-<tenant>-<project> for EVERY destination, including the
     in-cluster source-namespace destination. No destination may
     render without a matching entry.

5. TESTS: update tests/policy-tests.sh — replace name-prefix cases
   with label-binding cases (deny CR whose spec.tenant mismatches its
   namespace label; deny edits to guarded mgmt-ns labels; deny dup
   (tenant,name) across mgmt namespaces; combined-length and
   reserved-slug denials), and add: rendered source namespace carries
   the mgmt-namespace pointer; rendered AppProject has a
   destinationServiceAccounts entry per destination; the source-ns SA
   and RoleBinding render. Follow existing conventions (unique
   identities, assert deny reasons, cleanup).

Verify per AGENTS.md on the validate cluster: RGD Active, preflight
passes, full policy-tests pass.
